describe('nxt.CommandCell', function () {
	'use strict';

	describe('bind', function () {
		it('creates a command binding with command cell as a target', function () {

		});

		it('stores created binding in the bindings hash map', function (done) {

		});
	});

});
